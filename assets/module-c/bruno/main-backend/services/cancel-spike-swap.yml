info:
  name: cancel-spike-swap
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/services/{{spikeSwapServiceId}}/cancel"
  headers:
    - name: Authorization
      value: Bearer sl_tok_rider-003
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("cancel spike-skip reservation so rider-003 is free", function() {
          expect(res.status).to.equal(200);
          expect(res.body.state).to.equal("CANCELLED");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
