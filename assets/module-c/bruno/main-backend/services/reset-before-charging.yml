info:
  name: reset-before-charging
  type: http
  seq: 18

http:
  method: POST
  url: "{{stationServiceBaseUrl}}/reset"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Station Service reset DB before charging flow", function() {
          expect(res.status).to.equal(200);
          expect(res.body.status).to.equal("ok");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
