info:
  name: reset-before-conflict
  type: http
  seq: 14

http:
  method: POST
  url: "{{stationServiceBaseUrl}}/reset"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Station Service reset DB before confirm/cancel conflict cases", function() {
          expect(res.status).to.equal(200);
          expect(res.body.status).to.equal("ok");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
