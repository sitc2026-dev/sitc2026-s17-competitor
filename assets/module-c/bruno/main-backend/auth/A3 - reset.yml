info:
  name: "A3 - reset"
  type: http
  seq: 1

http:
  method: POST
  url: "{{stationServiceBaseUrl}}/reset"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("A3 - Station Service POST /reset restores the Module C MySQL seed", function() {
          expect(res.status).to.equal(200);
          expect(res.body.status).to.equal("ok");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
