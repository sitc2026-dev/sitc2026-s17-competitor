info:
  name: "B1 - login"
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/auth/login"
  body:
    type: json
    data: '{ "email": "lin.xiaoyu@swaploop.test", "password": "password123" }'
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("B1 - POST /auth/login returns 200 with opaque token for a seed rider", function() {
          expect(res.status).to.equal(200);
          expect(res.body.token).to.be.a("string");
          expect(res.body.token).to.equal("sl_tok_rider-001");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
