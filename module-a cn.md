# 測試專案大綱 – 模組 A – 小型測試專案

## 競賽時間

選手完成本模組的時間為 **3 小時**。

## 簡介

本專案由數個**小型速度測試專案**組成，用以評量靜態網站設計、前端開發，以及基礎後端網頁技術的核心能力。選手必須使用所提供的媒體檔案與起始程式碼，完成多個小型、可獨立運作的任務。每個小型測試專案都聚焦於網頁開發的特定面向，例如動畫、視覺效果、互動元件，或簡單的伺服器端功能。

選手必須精確遵循原始說明、在指定處使用所提供的素材，並確保每個小型測試專案都能作為獨立、可測試的單元實作完成。

## 專案與任務總述

你必須為模組 A – 靜態網站設計建立並整理一組**小型速度測試專案**。這些小型專案涵蓋：

- 使用 HTML 與 CSS 的**設計實作任務**。
- 使用 HTML、CSS 與 JavaScript 的**前端開發任務**。
- 使用 PHP 的**後端開發任務**，包含簡單的網頁應用程式行為與 API 相關設定。

每個小型測試專案都以獨立資料夾交付，內含各自的媒體檔案，部分任務也會提供起始程式碼。你必須：

- 依照引用方式，原樣使用所提供的媒體檔案。
- 讓每個小型測試專案保持獨立、自成一體。
- 只包含所需的小型測試專案檔案與資料夾，不要加入無關內容。

必須提供一個索引頁，連結到每一個小型速度測試專案，方便導覽與總覽。

請依下列結構繳交作品，其中每個資料夾都以該小型測試專案的識別碼命名：

```text
/
├── index.html   連結到每個小型測試專案的索引頁
├── A1/          Floating Label
├── A2/          Progress Animation
├── A3/          3D Can Rotation Effect
├── B1/          World's Tallest Buildings
├── B2/          Turntable
├── B3/          Lines and Dots Animation
├── C1/          Registration Form Validation
└── C2/          API Request Logger
```

每個小型測試專案的媒體檔案位於 `assets/<identifier>/`，例如 `assets/A1/`。請將所需媒體檔案複製到你自己的小型測試專案資料夾中。

後端小型測試專案使用 **PHP 8.x** 內建網頁伺服器執行，並從該小型測試專案的資料夾啟動：

```shell
php -S localhost:8080
```

## 等級制度

每個小型測試專案會歸入三個難度等級之一，等級同時決定預期完成時間與配分：

- **等級 1**（L1）：0.75 分，預計約 10 分鐘
- **等級 2**（L2）：1.5 分，預計約 20 分鐘
- **等級 3**（L3）：2.25 分，預計約 30 分鐘

每個類別最少 2 題、最多 4 題，並採用下列其中一種組合：

- 4 x L1
- 2 x L1 + 1 x L2
- 2 x L2
- 1 x L1 + 1 x L3

本測試專案採用以下組成：

| 類別     | 小型測試專案                  | 組合            | 配分   |
| -------- | ----------------------------- | --------------- | ------ |
| 設計實作 | A1 (L1)、A2 (L1)、A3 (L2)     | 2 x L1 + 1 x L2 | 3      |
| 前端開發 | B1 (L1)、B2 (L3)、B3 (L1)     | 2 x L1 + 1 x L3 | 3.75   |
| 後端開發 | C1 (L1)、C2 (L2)              | 1 x L1 + 1 x L2 | 2.25   |

## 需求

以下各節說明個別的小型速度測試專案。請仔細遵循所有說明，並以所提供的媒體素材作為參考。

### 一般需求

- 建立一個**索引頁**，連結到每一個小型速度測試專案。
- 索引頁必須為每個小型速度測試專案提供**縮圖**與**標題**。
- 每個小型速度測試專案至少要有**一次 git commit**。
- Git commit 訊息必須**易讀**且**容易理解**。

### 設計實作

此類任務以**僅使用 HTML 與 CSS** 重現小型視覺介面或動態效果。請盡可能貼近所提供的參考媒體：版面、色彩、字體、動畫，以及 hover、focus 或核取方塊切換等由 CSS 驅動的互動。此類任務**不得使用 JavaScript**。

#### A1：浮動標籤（等級 1）

僅使用 HTML 與 CSS，依照所提供的參考影片實作帶有**浮動標籤**的文字輸入框。

- 參考影片：`assets/A1/input.mp4`

當輸入框為空且未取得焦點時，標籤顯示在輸入框內部。當使用者將焦點移入輸入框時，標籤會移到欄位上緣、變小，並改變顏色。一旦輸入框含有文字，即使失去焦點，標籤也會維持在浮動位置。

**需求說明**

- 輸入框與其標籤的顯示效果須符合 `assets/A1/input.mp4`。
- 當輸入框為空且未取得焦點時，標籤顯示在輸入框內部。
- 當輸入框取得焦點時，標籤會以動畫移到欄位上緣並變小。
- 當輸入框含有文字時，即使失去焦點，標籤仍維持在浮動位置。
- 此效果僅以 HTML 與 CSS 實作，不得使用 JavaScript。

#### A2：進度動畫（等級 1）

完成所提供的頁面，使視窗頂端的**進度條**反映捲動位置。僅可使用 HTML 與 CSS。

- 提供的 HTML：`assets/A2/index.html`
- 提供的 CSS：`assets/A2/style.css`

當頁面捲動到頂端時，進度條寬度必須為 **0%**；捲動到底端時必須為 **100%**。使用者捲動時，進度條必須隨之更新。你可以完成所提供的 `style.css`，必要時也可加入僅使用 CSS 的標記（例如隱藏的核取方塊）；**不得**加入 JavaScript。

**需求說明**

- 以所提供的 `index.html` 結構作為起點。
- 進度條以固定位置顯示在視窗頂端。
- 頁面位於頂端時，進度條寬度為 0%。
- 頁面捲動到底端時，進度條寬度為 100%。
- 在這兩個極端之間，進度條寬度會跟隨捲動位置變化。
- 此效果僅以 HTML 與 CSS 實作，不得使用 JavaScript。

#### A3：3D 罐子旋轉效果（等級 2）

僅使用 CSS，依照影片範例建立 3D 罐子旋轉效果。

一開始，罐子出現在畫面中央。背景為 `background: radial-gradient(at 50%, #081718 -20%, #28aeb4 90%);`。當使用者將滑鼠移到罐子上時，會觸發旋轉與縮放動畫。

媒體檔案中提供了影片範例、罐子模型，以及必須套在罐子上的外包裝。請盡可能貼近影片範例中的效果。

**需求說明**

- 罐子顯示時，外包裝疊在上方。
- 使用正確的顏色。
- 外包裝正確套用到罐子上。
- 滑鼠移到罐子上時，罐子會旋轉。

### 前端開發

此類任務以 JavaScript 加入**用戶端行為**，並搭配 HTML 與 CSS。所列互動必須在瀏覽器中運作，且不依賴任何伺服器端邏輯。若有提供起始程式碼與媒體檔案，請使用它們。

#### B1：世界最高建築（等級 1）

你會取得一份含有部分世界最高建築資料的 JavaScript 檔，以及每座塔的圖片。

請使用該資料，在頁面上動態呈現建築天際線：

1. 依高度排序建築，最高者在前。
2. 縮放每張圖片，使最高建築高度為 800px，其餘建築依實際高度等比例縮放。
3. 維持每張圖片的長寬比。
4. 將所有建築對齊到頁面底部。

結果應呈現一排並肩而立的高塔，縮放與高度皆正確。

**需求說明**

- 建築會顯示在畫面上。
- 建築對齊正確。
- 建築高度正確。

#### B2：黑膠唱盤（等級 3）

製作一台**唱片機**的趣味動畫，讓使用者可以聆聽本週熱門歌曲。請使用所提供的起始頁面、圖片與音訊檔，並依照參考影片實作。

- 提供的 HTML：`assets/B2/index.html`
- 提供的 CSS：`assets/B2/css/style.css`
- 圖片：`assets/B2/images/`（`armTone.svg`、`circle.svg`、`turntable.PNG`）
- 音訊檔：`assets/B2/audio/DrunkonSoju.wav`、`assets/B2/audio/LostSomewhere.wav`
- 參考影片：`assets/B2/video/turntable.mp4`

所提供的頁面已包含**轉盤**（`.wheel`）、**唱臂**、**Off** 按鈕、**下一首（`>>`）**按鈕，以及**音量**範圍輸入（最小值 0、最大值 1）。請加入 JavaScript（起始 HTML 會連結 `js/audio.js`；該檔案未提供）。你可以完成 `style.css` 中已定義的 CSS class，例如 `.animation`。

**需求說明**

- 使用者按下轉盤時，歌曲開始播放，效果須符合 `assets/B2/video/turntable.mp4`。
- 歌曲播放期間，轉盤會旋轉，唱臂會移到轉盤上方，效果須符合參考影片。
- **Off** 按鈕會完全停止歌曲，轉盤與唱臂回到原始狀態。
- 音量控制器最小值為 0、最大值為 1，使用者可用它調整歌曲音量。
- 按下**下一首（`>>`）**按鈕會切換到另一首提供的音訊。
- 歌曲結束時，唱臂動畫回到原始狀態。
- 唱盤在瀏覽器中運作，不依賴伺服器端邏輯。

#### B3：線條與圓點動畫（等級 1）

你會取得網站的一些基本樣式。請以此為基礎，盡量重現影片範例中的線條與圓點動畫效果。

共有 40 個圓點。頁面重新整理時，它們的位置與大小會隨機產生。顏色也會從所提供的陣列中隨機選取。

當使用者在畫面上移動游標時，會觸發此效果。

**需求說明**

- 圓點的位置與顏色皆為隨機出現。顏色從陣列中選取。
- 線條與圓點動畫效果運作正確。

### 後端開發

此類任務聚焦於**伺服器端或資料驅動行為**。可見的使用者介面可以很精簡；分數來自正確的輸出、正確的 HTTP 行為，以及正確的設定。每個小型測試專案都必須以 PHP 內建網頁伺服器執行，並且必須在程式碼或 `README.txt` 中說明評審要呼叫的 URL。

#### C1：註冊表單驗證（等級 1）

建立一個將資料送到**伺服器**進行驗證的表單。請使用所提供的起始檔案。

- 提供的 HTML：`assets/C1/index.html`
- 提供的 CSS：`assets/C1/style.css`
- 提供的樣式表：`assets/C1/bootstrap.min.css`
- 參考圖片：`assets/C1/capture.png`

一開始不顯示任何回饋，也沒有任何已填入的值。若任一值無效，顯示回饋並停留在表單。若所有值皆有效，顯示 `Success`。

驗證規則：

- **First name**：僅限字母 `a–z` 與 `A–Z`
- **Last name**：僅限字母 `a–z` 與 `A–Z`
- **Agree to terms and conditions**：必須勾選

驗證訊息：

- 有效的姓名欄位：`Looks good!`
- 無效的姓名欄位：`Please provide a valid name.`
- 未勾選的核取方塊：`You must agree before submitting.`

**需求說明**

- 表單一開始為空白欄位，且沒有驗證回饋。
- 驗證在伺服器端執行，因此直接送到端點的無效資料也會被拒絕。
- 每個有效的姓名欄位顯示訊息 `Looks good!`。
- 每個無效的姓名欄位顯示訊息 `Please provide a valid name.`。
- 若核取方塊未勾選，顯示訊息 `You must agree before submitting.`。
- 若每個欄位皆有效，頁面顯示 `Success`。

#### C2：API 請求記錄器（等級 2）

建立一個 **PHP 端點**，接受帶有 `application/json` 內容的 POST 請求，並將每次呼叫分別寫入獨立檔案。

每次呼叫該端點時，必須將請求主體儲存在記錄資料夾中的文字檔，檔名為 `HH-MM-SS-request.txt`，其中 `HH`、`MM`、`SS` 分別替換為該次請求當下的時、分、秒。請建立 `README.txt`，說明評審要呼叫的 URL，以及記錄檔存放位置。

此小型測試專案不提供媒體檔案。

**需求說明**

- 端點接受帶有 `Content-Type: application/json` 標頭的 POST 請求。
- 每次呼叫都會將收到的請求主體儲存在記錄資料夾中的獨立文字檔。
- 檔名包含該次請求的時、分、秒，並符合規定格式。
- 儲存的檔案包含收到的 JSON 內容。
- 使用其他 HTTP 方法或其他內容類型的請求，須以適當的 HTTP 狀態碼拒絕。
- `README.txt` 須說明要呼叫的 URL，以及記錄檔的位置。

### 其他

- 本專案將使用 **Google Chrome** 瀏覽器進行評分。

## 評分

本專案將依所提供的**評分表**評分，並在 Google Chrome 中測試每個小型速度測試專案。評審將確認：

- 所有必要的小型測試專案皆已實作，且可從索引頁進入。
- 功能與視覺行為符合說明與參考媒體。
- 技術限制獲得遵守（例如僅限 HTML/CSS 的任務、伺服器端驗證、正確使用所提供的資料檔）。
- 各小型專案的使用體驗連貫且可用。

## 配分

本專案配分如下：

| WSOS 區塊 | 說明             | 分數   |
| --------- | ---------------- | ------ |
| 1         | 工作組織與自我管理 | 2      |
| 2         | 溝通與人際技能   | 1      |
| 3         | 設計實作         | 3      |
| 4         | 前端開發         | 3.75   |
| 5         | 後端開發         | 2.25   |
| **合計**  |                  | 12     |
